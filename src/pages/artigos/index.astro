---
import { getCollection } from "astro:content";
import BaseLayout from "../../layouts/BaseLayout.astro";
import ArticleCard from "../../components/ArticleCard.astro";

const articles = await getCollection("articles", ({ data }) => {
    return data.draft !== true;
});
---

<BaseLayout
    title="Artigos - Radar F5"
    description="Tendências, curiosidades e atualizações do mundo tech."
>
    <section class="container hero-section">
        <div class="callout-box">
            <h1>Artigos</h1>

            <p>
                Tendências, curiosidades e atualizações do mundo tech.
            </p>
        </div>
    </section>

    <section class="container articles-grid">
        <h2>Todos os Artigos</h2>

        {
            articles.map((article) => (
                <ArticleCard
                    title={article.data.title}
                    description={article.data.description}
                    category={article.data.category}
                    image={article.data.image}
                    imageAlt={article.data.imageAlt}
                    href={`/artigos/${article.data.slug}/`}
                />
            ))
        }
    </section>
</BaseLayout>
