---
import { getCollection } from "astro:content";
import BaseLayout from "../../../layouts/BaseLayout.astro";
import ArticleCard from "../../../components/ArticleCard.astro";

const articles = await getCollection("articles", ({ data }) => {
    return data.draft !== true && data.language === "en";
});
---

<BaseLayout
    title="Articles - Radar F5"
    description="Reviews, comparisons and buying guides from Radar F5."
    lang="en"
    locale="en_US"
>
    <section class="container hero-section">
        <div class="callout-box">
            <h1>Articles</h1>

            <p>
                Reviews, comparisons and buying guides to help you make better
                purchasing decisions.
            </p>
        </div>
    </section>

    <section class="container articles-grid">
        <h2>All Articles</h2>

        {
            articles.length > 0 ? (
                articles.map((article) => (
                    <ArticleCard
                        title={article.data.title}
                        description={article.data.description}
                        category={article.data.category}
                        image={article.data.image}
                        imageAlt={article.data.imageAlt}
                        href={`/en/articles/${article.id}/`}
                    />
                ))
            ) : (
                <p>Our English articles will be available here soon.</p>
            )
        }
    </section>
</BaseLayout>
